続けて質問です。最新のVisual Studio CodeをmacOSのVoiceOverスクリーンリーダーで操作する場合、テキストエディタの読み上げに不具合があります。macOSのバージョンはSequoia 15.5です。
具体的には以下の手順で操作すると、読み上げに不具合が生じます。
1. テキストエディタに`const foo: number = 123;`と入力する。
2. このとき、テキストカーソルを行の銭湯へ移動してから、Option+左右カーソルキーで単語単位のカーソル移動を行う。
3. 「const」「foo」「number」の順番で読み上げるのを期待するところ、「const」「foo」「foo」と記号`:`をまたぐ単語が重複して読み上げされる。
上記の不具合について、VoiceOverが単語を読み上げるまでの一連の流れをソースコードを示しつつ解説してください。手元にはmicrosoft/vscodeリポジトリをcloneしてあるので、実際に動作を試しながら確認できるような解説が望ましいです。
そして、解説の後、不具合の修正方法を検討し提案してください。
