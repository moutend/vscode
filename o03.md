## VoiceOver が同じ単語を二度読むまでの流れ
以下は`Option+←/→`（Word左右移動）で`const foo: number`を読み上げる際に発生する主なコールパスです。手元のcloneで各ファイルにブレークポイントや`console.trace()`を入れると追えます。
1. キーバインド→カーソル移動
* `cursorWordLeft/Right`コマンド（`wordOperations.ts`）が実行され、`editor._getViewModel().setCursorStates()`に`CursorChangeReason.Explicit`を渡す。([raw.githubusercontent.com][1])
* `USUAL_WORD_SEPARATORS`（`wordHelper.ts`）で`:`が語境界扱いのため、`foo`→`:`→`number`の3ステップになる。([raw.githubusercontent.com][2])
2. カーソルイベントの発火
* `CodeEditor.onDidChangeCursorPosition`が`reason === Explicit`で発火。
3. アクセシビリティ信号の生成
* `EditorTextPropertySignalsContribution`は`args.reason !== Explicit && ... !== NotSet`を除外し、`Explicit`のときだけ信号を出す。([raw.githubusercontent.com][3])
* これにより毎単語移動ごとに`AccessibilitySignalService.playSignal()`が呼ばれる。既定では`navigation`系のシグナル（音＋アナウンス）が有効。
4. ARIAライブリージョンへの書き込み
* `AccessibilitySignalService`が`accessibilityService.status()`を実行。ここで同じ単語名を`aria-live = "polite"`コンテナに挿入する。([raw.githubusercontent.com][4])
* DOM書き込みは`aria/aria.ts`の`status()`。ダブルバッファ（`statusContainer`/`statusContainer2`）で同一語を繰り返し読ませる仕組み。([raw.githubusercontent.com][5])
5. スクリーンリーダー自然読み上げ
* 同時に`ScreenReaderSupport`が不可視テキストエリアを更新し、VoiceOverはカーソル位置の語をOSネイティブ経由で読み上げる。([raw.githubusercontent.com][6])
6. 結果
* 1ステップで自然読み上げ＋ARIAアナウンスの二重発話。
* `:`で区切られているため
* カーソルは`foo`に着地→2回読まれる
* 次は `:`（VoiceOver は「コロン」と読む）→ VS Code 側は記号を読み上げず無音
* さらに `number` → 単発で読まれる
       という並びになり、体感上「const / foo / foo」となる。
## 再現手順のヒント
```bash
# 例: 中核関数に console.log を仕込む
cd vscode
code .
# src/vs/workbench/contrib/accessibilitySignals/…/editorTextPropertySignalsContribution.ts
# - onDidChangeCursorPosition 内で console.log(args.reason, position);
# デバッグビルド: yarn gulp vscode-darwin
# 起動して Option+→ でトレースを確認
```
## 修正方針の提案
### 1. macOS + VoiceOver ではナビゲーション信号を抑制する
```ts
// editorTextPropertySignalsContribution.ts 内
if (isMacintosh && accessibilityService.isScreenReaderOptimized()) {
    // ナビゲーション時は信号を送らない
    return;
}
```
* 利点: 既存コード最小変更。
* 欠点: 他の信号（エラー行到達など）は残したい場合に調整が必要。
### 2. カーソル移動に専用 Reason を導入
* `CursorChangeReason.Navigation` のような新値を追加し、
  Word 移動コマンド側を `Navigation`、クリックや選択は `Explicit` に分離。
* `EditorTextPropertySignalsContribution` は `Explicit` のみを対象にして
  読み上げ対象を限定する。
* 利点: プラットフォーム依存せず意図を明確化。
* 欠点: API 互換性に影響するため広範囲の修正とレビューが必要。
### 3. ユーザー設定による回避策
```jsonc
// settings.json
"accessibilitySignals.cursorNavigation.announcement": "off",
"accessibilitySignals.cursorNavigation.sound": "off"
```
* 既存 VS Code をパッチ不要で回避可能。
* デフォルトを macOS では `"off"` にする PR を提案する。
### 4. コードパッチ例（最小実装）
```diff
diff --git a/src/vs/workbench/contrib/accessibilitySignals/browser/editorTextPropertySignalsContribution.ts b/...
@@
-store.add(editor.onDidChangeCursorPosition(args => {
-    timeouts.clear();
-    if (args && args.reason !== CursorChangeReason.Explicit && args.reason !== CursorChangeReason.NotSet) {
+store.add(editor.onDidChangeCursorPosition(args => {
+    timeouts.clear();
+    const shouldAnnounce =
+        args?.reason === CursorChangeReason.Explicit &&
+        !(isMacintosh && accessibilityService.isScreenReaderOptimized());
+    if (!shouldAnnounce) {
         return; // ← ナビゲーション時の発話を抑制
     }
```
---
上記いずれかの方法で 「自然読み上げ＋ARIAアナウンスの二重発話」 を防止できます。開発版で試す場合は `--disable-extensions` で純粋な挙動を確認してから PR を送るとよいでしょう。
[1]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/editor/contrib/wordOperations/browser/wordOperations.ts "raw.githubusercontent.com"
[2]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/editor/common/core/wordHelper.ts "raw.githubusercontent.com"
[3]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/workbench/contrib/accessibilitySignals/browser/editorTextPropertySignalsContribution.ts "raw.githubusercontent.com"
[4]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/accessibilitySignal/browser/accessibilitySignalService.ts "raw.githubusercontent.com"
[5]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/base/browser/ui/aria/aria.ts "raw.githubusercontent.com"
[6]: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/editor/browser/controller/editContext/native/screenReaderSupport.ts "raw.githubusercontent.com"
